{
  "name": "remotedesk-server",
  "version": "0.1.0",
  "private": true,
  "description": "Signaling + TURN-credential server for RemoteDesk (TeamViewer-like)",
  "main": "src/index.js",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "lint": "eslint src",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "better-sqlite3": "^11.10.0",
    "cookie": "^1.1.1",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "eslint": "^9.10.0"
  }
}
